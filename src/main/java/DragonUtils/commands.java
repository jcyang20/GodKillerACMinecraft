// 暂时废弃,请勿使用

/*
package DragonUtils;

import org.bukkit.command.PluginCommand;
import org.bukkit.plugin.Plugin;

public class commands
{
    public static boolean registerCommands(Plugin plugin)
    {
        try
        {
            //PluginCommand newcommand=new PluginCommand("424",plugin);
            return true;
        }
        catch(Exception e)
        {
            return false;
        }
    }
}
*/